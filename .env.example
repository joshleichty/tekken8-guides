# Optional: production notes API override (defaults to /api/notes)
VITE_NOTES_API_URL=

# Local dev target for notes API. Point this to your deployed Vercel endpoint
# if you run only Vite locally.
# Example: VITE_NOTES_API_URL_DEV=https://your-site.vercel.app/api/notes
VITE_NOTES_API_URL_DEV=
